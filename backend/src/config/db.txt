DB chalane ke liye 
npm db:init karo, agar nahi chala toh package.json gpt mei paste karke db:init chalane bolo,
stack builder se postgres mei postgis ka extension download karna padega pehle, documentation se dekho varna gpt karo
//

CREATE DATABASE runsphere;

-- Enable the extension to generate UUIDs
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- Enable PostGIS now so it's ready for the maps feature later
CREATE EXTENSION IF NOT EXISTS postgis; 

-- Create the users table exactly as designed in your schema
CREATE TABLE IF NOT EXISTS users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    username VARCHAR(255) UNIQUE NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    avatar_url TEXT,
    bio TEXT,
    level INTEGER DEFAULT 1,
    xp INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);